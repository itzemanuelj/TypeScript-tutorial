/// TypeScript Special Types

/// Type: any

/// Setting any to the special type any disables type checking:
/// any type will overide ts default

let q: any = false;
q = "any";

/// Type: unknown

let w: unknown = false;

/// Type: never

//let ebook: never = 'never';

/// Type: undefined & null;

let y: undefined = undefined;
let z: null = null;

/// Create an empty "myVar" variable, and disable type checking:

let myVar: any = {};

/// Create a "firstName" variable, string type using Explicit type:

let firstNameName: string = "jusitn";

/// Create an empty "myVar" variable, and disable type checking:

let myVariable: any = "true";

/// Create an empty "myVar" variable, and specify it should be an unknown type:

let myVariable1: unknown = "true";
